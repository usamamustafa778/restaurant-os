# RestaurantOS Dashboard – required credentials
# Copy this file to .env and set values. Do not commit .env to git.

# Backend API base URL (no trailing slash); must match backend PORT
NEXT_PUBLIC_API_BASE_URL=http://localhost:5001

# Must match backend JWT_SECRET so middleware can verify login tokens
JWT_SECRET=restaurantos_dev_secret_change_in_production_2026
FIX_SUPER_ADMIN_EMAIL=superadmin@restaurantos.local
